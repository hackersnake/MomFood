## Example .env for MomFood backend (Backend/momfood)
# Copy to `.env` and update values for your local setup.

# JDBC URL for MySQL
DB_URL=jdbc:mysql://localhost:3306/momfood_db?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=your_mysql_password

# Server port
SERVER_PORT=8080
